# Build output
bin/
obj/
TestResults/

# Visual Studio
.vs/
*.user
*.suo

# Installer output (large binaries) - **/ so it matches at any depth
**/Installer/Output/

# Legacy Visual Studio installer project output (binaries may embed secrets)
Setup1/Debug/
Setup1/Release/

# Local Claude settings
**/.claude/settings.local.json

# Local-only default AI key (never commit; the app reads it from beside the exe)
openrouter-default.key
